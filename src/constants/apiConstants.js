export const API_URL = "https://shabtsak.onrender.com";

export const CAMP_URL = `${API_URL}/camp`;
export const OUTPOST_URL = `${API_URL}/outpost`;
export const GUARD_URL = `${API_URL}/guard`;
export const SHIFT_URL = `${API_URL}/shift`;
